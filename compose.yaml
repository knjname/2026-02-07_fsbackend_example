# ビルド定義のみ。開発環境セットアップは devenv/compose.yml を使うこと。
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: api

  batch:
    build:
      context: .
      dockerfile: Dockerfile
      target: batch

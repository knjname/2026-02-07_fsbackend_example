services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: fsapi
      POSTGRES_USER: fsapi
      POSTGRES_PASSWORD: fsapi
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
